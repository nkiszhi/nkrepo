# Scan samples using Kaspersky avtool

read\_log.py extracts scan results from Kaspersky log file and saves scan result into nkrepo.

read.py appends new scan results to the result file.

delete.py deletes samples scaned by kav. 
